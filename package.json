{
  "name": "floweret",
  "version": "0.7.3",
  "description": "Runtime signature type checker using native JavaScript types.",
  "author": "Laurent Payot",
  "license": "MIT",
  "homepage": "https://github.com/laurentpayot/floweret#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/laurentpayot/floweret"
  },
  "keywords": [
    "type-checking",
    "type-checker",
    "type",
    "check",
    "typed",
    "signature",
    "runtime",
    "dynamic",
    "flow",
    "flow-runtime",
    "runtypes",
    "javascript",
    "typescript",
    "coffeescript",
    "coffee"
  ],
  "engines": {
    "node": ">=7.6.0"
  },
  "scripts": {
    "build": "rm -rf dist && coffee -ctb -o dist src",
    "test": "nyc --extension .coffee mocha --require @babel/register --require ./coffeescript.config.js \"test/**/*.{js,coffee}\"",
    "test-watch": "mocha --reporter min --require @babel/register --require ./coffeescript.config.js --watch --watch-extensions js,coffee,json \"test/**/*.{js,coffee}\"",
    "coverage": "nyc report --reporter=text-lcov | coveralls",
    "bundle": "npm run build && cd benchmark && rollup -c",
    "sizes-webpack": "cd benchmark && echo 'No type Checking:' && size-limit no-type-checking.js && echo 'Floweret:' && size-limit floweret.js && echo 'Runtypes:' && size-limit runtypes.js && echo 'Flow-runtime:' && size-limit flow-runtime.js",
    "sizes-rollup": "cd benchmark/bundles && stat -c '%n %s bytes' *.gz | sort -n -k 2 | column -t && echo",
    "benchmark": "npm run bundle && npm run sizes-rollup && cd benchmark/bundles && node no-type-checking-benchmark.min.js && node floweret-benchmark.min.js && node runtypes.min.js && node flow-runtime-benchmark.min.js && echo",
    "link": "mkdir -p dist && cp package.json dist && cd dist && npm link && rm -rf node_modules && rm package-lock.json && cd .. && npm link floweret",
    "preversion": "npm test && npm run build",
    "postversion": "git push && git push --tags && cp package.json dist && cp *.md dist && npm publish",
    "size": "size-limit"
  },
  "dependencies": {},
  "devDependencies": {
    "@babel/core": "^7.1.2",
    "@babel/register": "^7.0.0",
    "babel-plugin-transform-es2015-modules-simple-commonjs": "^0.3.0",
    "chai": "^4.2.0",
    "chai-as-promised": "^7.1.1",
    "coffeescript": "^2.3.2",
    "coveralls": "^3.0.2",
    "flow-runtime": "^0.17.0",
    "mocha": "^5.2.0",
    "nyc": "^13.0.1",
    "rollup": "^0.66.6",
    "rollup-plugin-commonjs": "^9.2.0",
    "rollup-plugin-gzip": "^2.0.0",
    "rollup-plugin-node-resolve": "^3.4.0",
    "rollup-plugin-terser": "^3.0.0",
    "runtypes": "^2.1.6",
    "sinon": "^6.3.5",
    "size-limit": "^0.20.1"
  },
  "size-limit": [
    {
      "limit": "5 KB",
      "path": "dist/index.js"
    }
  ],
  "nyc": {
    "exclude": [
      "babel.config.js",
      "coffeescript.config.js",
      "test/**/*"
    ]
  }
}
