{
  "name": "floweret",
  "version": "0.5.2",
  "description": "A runtime signature type-checker using native JavaScript types.",
  "author": "Laurent Payot",
  "license": "MIT",
  "homepage": "https://github.com/laurentpayot/floweret#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/laurentpayot/floweret"
  },
  "keywords": [
    "type-checking",
    "type-checker",
    "type",
    "check",
    "typed",
    "signature",
    "runtime",
    "flow",
    "flow-runtime",
    "runtypes",
    "javascript",
    "typescript",
    "coffeescript",
    "coffee"
  ],
  "engines": {
    "node": ">=7.6.0"
  },
  "scripts": {
    "build": "npm run build-ESM",
    "build-CJS": "rm -rf dist/ && coffee -ctmb -o dist/ src/ && npm run size",
    "build-ESM": "rm -rf dist/ && coffee -cmb -o dist/ src/ && npm run size",
    "build-tests": "coffee -ctm test/tests.coffee",
    "test": "npm run build-CJS && npm run build-tests && mocha",
    "bundle": "npm run build && cd benchmark && rollup -c && echo && cd bundles && stat -c '%n %s bytes' *.gz | sort -n -k 2 | column -t",
    "benchmark": "npm run bundle && cd benchmark/bundles && node no-type-checking-benchmark.min.js && node floweret-benchmark.min.js && node runtypes.min.js && node flow-runtime-benchmark.min.js && echo",
    "link": "npm run test && cp package.json dist && cp *.md dist && cd dist && npm link",
    "preversion": "npm test",
    "postversion": "git push && git push --tags && cp package.json dist && cp *.md dist && cd dist && rm -r **/*.map && npm publish",
    "size": "size-limit"
  },
  "size-limit": [{
    "limit": "4 KB",
    "path": "dist/index.js"
  }],
  "devDependencies": {
    "@babel/core": "^7.1.2",
    "babel-plugin-transform-es2015-modules-simple-commonjs": "^0.3.0",
    "chai": "^4.2.0",
    "chai-as-promised": "^7.1.1",
    "coffeescript": "^2.3.2",
    "flow-runtime": "^0.17.0",
    "mocha": "^5.2.0",
    "rollup": "^0.66.4",
    "rollup-plugin-commonjs": "^9.1.8",
    "rollup-plugin-gzip": "^2.0.0",
    "rollup-plugin-node-resolve": "^3.4.0",
    "rollup-plugin-terser": "^3.0.0",
    "runtypes": "^2.1.6",
    "sinon": "^6.3.5",
    "size-limit": "^0.20.1"
  },
  "dependencies": {}
}
