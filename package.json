{
  "name": "runtime-signature",
  "version": "0.0.11",
  "description": "Runtime type-checking with simple JavaScript types signatures.",
  "author": "Laurent Payot",
  "license": "MIT",
  "homepage": "https://github.com/laurentpayot/runtime-signature#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/laurentpayot/runtime-signature"
  },
  "keywords": [
    "type-checking",
    "type",
    "check",
    "typed",
    "signature",
    "runtime",
    "flow",
    "javascript",
    "typescript",
    "coffeescript",
    "coffee"
  ],
  "main": "dist/index.js",
  "engines": {
    "node": ">=7.6.0"
  },
  "scripts": {
    "build": "rm -rf dist/ && coffee -cb -o dist/ src/",
    "build-tests": "coffee -cb test/tests.coffee",
    "test": "npm run build && npm run build-tests && mocha",
    "deploy": "npm run test && npm publish"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "chai-as-promised": "^7.1.1",
    "coffeescript": "^2.3.1",
    "mocha": "^5.2.0",
    "uglify-es": "^3.3.9"
  }
}
